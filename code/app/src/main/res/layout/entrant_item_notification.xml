<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Bell icon -->
    <FrameLayout
        android:id="@+id/bellContainer"
        android:layout_width="28dp"
        android:layout_height="28dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iconBell"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/ic_notifications"
            app:tint="#222222"/>

        <!-- Dot anchored to top|start, above the bell -->
        <View
            android:id="@+id/dotUnread"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:background="@drawable/dot_red"
            app:layout_constraintTop_toTopOf="@id/iconBell"
            app:layout_constraintStart_toStartOf="@id/iconBell"
            android:translationX="2dp"
            android:translationY="2dp"
            android:elevation="8dp"/>

    </FrameLayout>

    <!-- Buttons (right column) -->
    <LinearLayout
        android:id="@+id/actions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAccept"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="Accept"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            app:cornerRadius="20dp"
            app:backgroundTint="@android:color/black"
            app:iconPadding="0dp"
            app:backgroundTintMode="src_over" />   <!-- ✨ key line to enforce solid color -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDecline"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginTop="12dp"
            android:text="Decline"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            app:cornerRadius="20dp"
            app:backgroundTint="#F00028"
            app:backgroundTintMode="src_over" />   <!-- ✨ also needed -->

    </LinearLayout>

    <!-- Text block (center) -->
    <LinearLayout
        android:id="@+id/textBlock"
        android:layout_width="240dp"
        android:layout_height="132dp"
        android:layout_marginEnd="12dp"
        android:orientation="vertical"
        app:layout_constraintEnd_toStartOf="@id/actions"
        app:layout_constraintStart_toEndOf="@id/iconBell"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Title row: bold title + grey time -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:letterSpacing="0.01"
                android:text="Congratulations!"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="4d"
                android:textColor="#9E9E9E"
                android:textSize="16sp" />
        </LinearLayout>

        <!-- Subtitle (grey) -->
        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Beginner Swim Lessons"
            android:textColor="#9E9E9E"
            android:textSize="16sp" />

        <!-- Body -->
        <TextView
            android:id="@+id/tvBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:lineSpacingExtra="2dp"
            android:text="You have been selected for Beginner Swim Lessons hosted by shadowlynx!"
            android:textSize="18sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
